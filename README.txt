How to run services with docker-compose

docker-compose -f <file.yml> create <name>
docker-compose -f <file.yml> start
docker-compose -f <file.yml> scale <name>=<n>
docker-compose -f <file.yml> stop <name>

